import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  text: {
    paddingTop: 20,
    paddingLeft: 15,
    paddingBottom: 20,
    fontSize: 20,
  },
});

export default styles;
